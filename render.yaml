services:
  - type: web
    name: equinesync-demo
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python src/demo_data_loader.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0
      - key: PORT
        value: 8000
